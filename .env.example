# Deployment Configuration
# Copy this to .env and fill in your values
# Then run: source .env (or set these in your system environment)

# EC2 Instance Configuration
EC2_IP=18.208.117.82
EC2_USER=ec2-user
SSH_KEY_PATH=/path/to/your/key.pem

# GitHub Repository
GIT_REPO=https://github.com/tchamna/resulam_royalties.git

# Application Configuration
APP_PORT=8050
APP_DIR=/home/ec2-user/apps/resulam-royalties
DOMAIN_NAME=resulam-royalties.tchamna.com

# AWS Configuration (for S3 data files)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
S3_BUCKET=resulam-royalties

# Optional: S3 data files to download (pipe-separated, or leave empty)
# S3_DATA_FILES=file1.csv|file2.xlsx
# Leave empty to use defaults or skip S3

# Optional: Additional Python packages (pipe-separated, or leave empty)
# PYTHON_PACKAGES=package1==1.0|package2==2.0
# Leave empty to use only requirements.txt

# Processed data export (local)
# EXPORT_PROCESSED_DATA=false

# Chatbot (optional local LLM via Ollama)
# CHATBOT_ENGINE=rules
# CHATBOT_USE_LLM=true
# CHATBOT_LLM_PROVIDER=ollama
# CHATBOT_OLLAMA_URL=http://localhost:11434
# CHATBOT_LLM_MODEL=llama3.2:3b
# CHATBOT_OLLAMA_TIMEOUT=20
# CHATBOT_RESULT_LIMIT=12
# CHATBOT_RAG_ENABLED=true
# CHATBOT_RAG_INDEX_PATH=data/chatbot_rag_index.pkl
# CHATBOT_RAG_TOP_K=30

# ============================================================================
# How to use:
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your values
#
# 3. Load environment variables (choose one):
#
#    Windows (PowerShell):
#      $env:EC2_IP="18.208.117.82"
#      $env:SSH_KEY_PATH="C:\path\to\key.pem"
#      # ... set others
#
#    macOS/Linux (Bash):
#      export $(cat .env | grep -v '#' | xargs)
#      # or
#      source .env
#
#    Or use a tool like direnv:
#      brew install direnv
#      echo "source_env .env" > .envrc
#      direnv allow
#
# 4. Run deployment:
#    python deploy.py
#
# The script will automatically use your environment variables!
